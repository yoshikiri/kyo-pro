#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main() {
  int a, p;
  cin >> a >> p;
  int sum = 3 * a + p;
  cout << sum / 2 << endl;
  return 0;
}
