#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main() {
  int a;
  string s;
  cin >> a >> s;

  if (a < 3200)
    cout << "red" << endl;
  else
    cout << s << endl;
  return 0;
}
