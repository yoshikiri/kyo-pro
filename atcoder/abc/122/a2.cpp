#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main() {
  char c;
  cin >> c;
  if (c == 'A') cout << 'T' << endl;
  if (c == 'C') cout << 'G' << endl;
  if (c == 'G') cout << 'C' << endl;
  if (c == 'T') cout << 'A' << endl;
  return 0;
}
