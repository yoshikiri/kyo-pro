#include <bits/stdc++.h>

using namespace std;
using ll = long long;

#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

int main() {
  char c;
  cin >> c;
  if (c >= 'a' && c <= 'z')
    cout << 'a';
  else
    cout << 'A' << endl;
  return 0;
}
